# Lo que hace falta para compilar y publicar este sitio.
# Instalar con:  pip install -r requirements.txt
#
# Las versiones estan fijadas a proposito: son las mismas que usa la PC de
# escritorio. Si una maquina instala una version distinta de mkdocs-material,
# el HTML generado puede cambiar y ensuciar los commits con diferencias que
# no tienen nada que ver con lo que se edito.

mkdocs==1.6.1
mkdocs-material==9.7.5
mkdocs-static-i18n==1.3.1
pymdown-extensions==10.21
